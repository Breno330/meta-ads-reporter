# Dependências
node_modules/

# Secrets — NUNCA subir para o repositório
.env
.env.hml
.env.prod

# Dados dos clientes (criptografados localmente, configurados no Render via Persistent Disk)
data/

# Logs
*.log
logs/

# Scripts de ambiente local (específicos da máquina)
start-tunnel.bat
start-tunnel.ps1

# PDF da identidade visual (não precisa ir para o repositório)
*.pdf

# Arquivos de sistema
.DS_Store
Thumbs.db
